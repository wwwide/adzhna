import { ReactElement } from 'react';
import { PaginationProps } from '../PaginationProps';
export declare const usePaginationMarkup: (props: PaginationProps) => ReactElement | ReactElement[] | null;
