export type SelectTheme = {
  // Padding in "rem".
  padding: {
    x: number
    y: number
  }
  borderRadius?: number
  dropDown: {
    shadow: string
    padding: {
      x: number
      y: number
    }
  }
}
